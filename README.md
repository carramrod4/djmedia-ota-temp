# djmedia-ota-temp

Temporary Ad Hoc OTA install hosting for D&J Media.

This repo (and its release) auto-deletes ~3 days after creation via the
scheduled GitHub Actions workflow in `.github/workflows/auto-delete-release.yml`.
